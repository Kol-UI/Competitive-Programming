Returns the angle whose cosine is the specified number.
```cs
public class Acos
{
    public static double BuiltInAcos(double d)
    {
        // Returns the angle in radians whose cosine is the specified number.
        // Input must be between -1 and 1, output is between 0 and PI.
        return Math.Acos(d);
    }
}```